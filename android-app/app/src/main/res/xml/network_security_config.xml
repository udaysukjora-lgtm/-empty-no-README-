<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext (http://) traffic to the Android emulator's host-loopback
  alias only, so debug builds can talk to a backend running on your machine
  at http://10.0.2.2:8000. Remove this whole file (and the manifest
  reference to it) once BASE_URL points at a real https:// backend.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
